using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DialogueHandler2 : MonoBehaviour
{
    //Niko Chow-Stuart

    //This is a placeholder script, it would take an input of text from another script (due to some action happening)
    //and process it so that the character name would get changed to who's speaking, and display the dialogue on the
    //dialogue box.

    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
